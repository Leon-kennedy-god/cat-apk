# 喵喵助手 发布说明（Release Notes）

## v1.2.0（versionCode 5）—— 微信兼容与输入框精确定位

### 修复一：全选删除时误填充主页顶部的搜索栏
- 原逻辑每次处理都会全窗扫描并选中"第一个可编辑节点"，可能在页面里有搜索栏 / 占位输入框时改写错对象。
- 现在改写对象改为**事件源节点**（即用户正在编辑的输入框）优先，并强制要求该节点**聚焦**后才改写；
  仅在没有事件源（如发送按钮兜底）时才做全窗扫描，且扫描结果同样必须聚焦，绝不把文字写进未聚焦的搜索栏。

### 修复二：微信输入框无法使用
- 微信等应用的聊天输入框是自定义复合 View，常被标记为"非重要 View"，导致节点树/文本变化事件不同步，TextWatcher 收不到变化，改写"没反应"。
- 新增两条兜底：
  1. 服务开启时追加 FLAG_INCLUDE_NOT_IMPORTANT_VIEWS 标志，把自定义输入控件纳入无障碍节点树；
  2. 内置轻量轮询兜底（默认 800ms），定期读取前台可编辑输入框做文本 diff，事件被吞也能改写；
  轮询复用事件路径的防抖时钟与幂等逻辑，不会重复改写，也不会干扰提示词拦截时序。

### 注意
- 微信下若仍需更保守的改写节奏，可继续使用"标点触发"模式。

### 下载链接（稳定直链）
- https://github.com/Leon-kennedy-god/cat-apk/releases/latest/download/MeowMeowAssistant.apk

---
## v1.1.2（versionCode 4）—— 提示词拦截全面重做

### 问题
v1.1.1 的防护要求"变化前文本为空"，但很多应用把「删除 → 显示提示词」合并成
一步替换事件（变化前非空），且发送按钮若是无文字图标（关键词匹配失败）时，
发送后的提示词仍会被改写。

### 修复（五层拦截）
1. **不再要求"变化前为空"**：纯内容+时序判定，合并替换事件也能拦；
2. **空状态追踪扩展**：事件、窗口读取、处理读取三条路径观察到输入框为空都记录；
3. **高信号提示词模式库**：说点什么/说两句/输入消息/写评论/添加评论/善语结善缘/
   请输入/评论一下/留下你的/说说你的/留言/吐槽一下/回复一下 等，不依赖时序直接拦截；
4. **占位文本记忆**：被拦截的提示词 10 秒内再次出现（任意路径）精确匹配跳过；
5. **发送按钮识别扩充**：新增 发表/发布/回复/评论/post/reply/comment/ok/done 关键词。

处理级（发送兜底）采用**严格模式**：只认"记忆匹配"或"模式命中 + 输入框刚清空"，
避免把用户刚打的真实短消息误判为提示词。

### 下载链接（稳定直链）
- https://github.com/Leon-kennedy-god/cat-apk/releases/latest/download/MeowMeowAssistant.apk

---

## v1.1.1（versionCode 3）—— 官方提示词拦截（事件级时序）

### 修复（事件级提示词防护，自动生效，无需配置）
之前的"仅处理聚焦输入框 + hint 相等检测"无法拦截**把提示词作为节点文本写入**的应用
（如抖音），因为清空/发送后输入框仍是聚焦状态、且提示词不走 hint。

新增**三重信号判定**（在文本变化事件层直接拦截）：
1. 记录"输入框文本刚变空"的时刻（按包名隔离）；
2. 记录"发送按钮刚点击"的时刻（按包名隔离）；
3. 当文本从"空"变"非空"、且是 3~40 字的短文本、且发生在上述事件 3 秒内
   → 判定为官方提示词（如"说点什么吧"），直接跳过，不再改写。

覆盖场景：
- 实时模式：清除输入框文字的瞬间出现的提示词；
- 标点触发模式：点击发送后输入框清空、提示词出现的瞬间；
- 语音输入结束后的首个流式分片（恰好也是"空→非空"短文本）也会被跳过。

对正常输入的影响：用户清空后立刻打的**第一个字**可能被跳过一次，下一事件立即恢复正常
（发送按钮兜底保证最终文本仍会被改写）。

### 下载链接（稳定直链，永不失效）
- https://github.com/Leon-kennedy-god/cat-apk/releases/latest/download/MeowMeowAssistant.apk

---

## v1.1（versionCode 2）—— 语音输入与提示词修复

### 修复
1. **提示词（placeholder）误识别**：抖音等应用把输入框提示词暴露为节点文本时会被改写。
   新增防护：
   - 「仅处理聚焦的输入框」（默认开启）：未聚焦输入框里的占位/后台文字一律不处理；
   - 提示词检测：节点文本与其 hint（提示词）一致时自动跳过。
2. **语音输入被打断**：语音识别的流式中间结果（未成型文字）会被实时改写。
   新增「流式输入稳定防抖」（默认 800ms）：实时模式下，输入变化间隔小于设定值视为
   "尚未成型"，等待输入停止后再改写；面板刚弹出时的占位文本事件也会落入防抖窗口。
   标点触发模式不受影响（流式中间结果极少以标点结尾，且防抖会吞掉最后的触发事件）。

### 新增配置（应用内「高级设置」区块）
- 仅处理聚焦的输入框（开关，默认开）
- 流式输入稳定防抖（毫秒，默认 800，填 0 关闭）

### 注意
- 实时模式配合防抖后，改写在"输入停顿 N 毫秒后"发生；需要立即改写可把防抖设为 0。
- 语音输入强烈建议保持默认防抖值 800ms。

---

## v1.0（versionCode 1）—— 首个发布

## 版本
- 版本号：v1.0（versionCode 1）
- 包名：`com.meow.assistant`
- 应用名：喵喵助手
- 平台：Android（minSdk 23 / targetSdk 34）

## 本版本内容（功能扩张声明）
本版本是 https://github.com/QiCaiJie114514/QQMiaoAssistant （QQ文本改写助手）的
**功能扩张（二次开发衍生）版本**：核心改写引擎与上游一致，仅在"作用范围"上做了扩张。

### 新增/扩张（vs 上游）
1. **任意聊天软件可用**：删除上游写死的 QQ 包名限制与 View ID 依赖，改为通用
   "可编辑节点"检索；目标应用白名单留空 = 所有应用；
2. **作用范围配置**：目标应用白名单（内置 QQ/微信/Telegram/WhatsApp/钉钉/Discord/
   飞书/Slack/LINE/Signal 一键填入）+ 排除黑名单；
3. **安全保护**：自动跳过输入法(IME)窗口与包名、桌面启动器、系统设置；密码框永不处理；
4. **发送按钮兜底**：由 QQ 专属 View ID 改为通用发送关键词识别，可开关；
5. **行为验证**：tools/ 目录提供 Java 引擎测试（27 项断言全部通过）与 Python 演示工具。

### 保留（与上游一致）
- 文本替换规则（支持 = ＝ → 三种分隔符）
- 断句追加（默认"喵"，可自定义）
- 句末随机猫咪颜文字（内置 50+，可自定义）
- 标点触发 / 实时处理两种模式
- 防反馈环（写回回显跳过）、用户原文增量跟踪

## 下载
- APK：`dist/喵喵助手-v1.0-debug.apk`（debug 签名，可直接侧载安装）

## 构建方式
```bash
./gradlew assembleDebug
```

## 已知边界
- 个别不暴露可编辑节点的应用（如部分游戏内置聊天）无法改写；
- 需要用户在系统"无障碍"设置中手动开启本服务；
- 微信等应用对无障碍写回有限流时，建议使用"标点触发"模式。

## 合规声明
本仓库为上游 QQMiaoAssistant（AGPL-3.0）的功能扩张衍生项目，详见 README 顶部声明与 NOTICE 文件。
上游作者联系 QQ：1670534177。
